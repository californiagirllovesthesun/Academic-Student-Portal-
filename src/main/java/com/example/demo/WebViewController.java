package com.example.demo;

// This controller has been disabled because PortalController handles active routing.
public class WebViewController {
    // Dormant file to prevent ambiguous mapping collisions
}
